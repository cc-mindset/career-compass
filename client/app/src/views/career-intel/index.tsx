import React from 'react';
import { UserProfile } from '../../types';

interface CareerIntelViewProps {
  user: UserProfile;
}

//empty placeholder — Career Intel content removed for redesign.
const CareerIntelView: React.FC<CareerIntelViewProps> = () => {
  return <div />;
};

export default CareerIntelView;
  